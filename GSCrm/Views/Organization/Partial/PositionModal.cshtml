@inject ResManager resManager
@model OrganizationViewModel
@using static GSCrm.CommonConsts

@* // Модальное окно создания должности *@
<div id="positionModal" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">@resManager.GetString(POSITION)</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form method="post" asp-controller="Position" asp-action="Create">
                    <div class="form-group">
                        <div class="mt-3">
                            <input id="positionName" class="form-control" type="text" placeholder="@resManager.GetString("NameLabel")" />
                            <ul id="positionNameError" data-connect-el="positionName" class="d-none under-field-error label-sm mt-2"></ul>
                        </div>

                        <div class="mt-4">
                            <div id="positionDivModal" class="select-autocomplete" data-link="@Url.Content($"~/Autocomplite/GetDivisions/{Model.Id}/")">
                                <input id="positionDivModalVal" class="form-control" placeholder="@resManager.GetString(DIVISION)" />
                            </div>
                        </div>
                        <ul id="positionDivModalError" data-connect-el="positionDivModalVal" class="d-none under-field-error label-sm mt-2"></ul>

                        <div class="mt-4">
                            <div id="parentPositionModal" class="select-autocomplete" data-link="@Url.Content($"~/Autocomplite/GetPositions/{Model.Id}/")"
                                 data-autocomplite-type="Positions" data-autocomplite-name="ParentPositionModal">
                                <input id="parentPositionModalVal" class="form-control" placeholder="@resManager.GetString("ParentPositionLabel")" />
                            </div>
                        </div>
                        <ul id="parentPositionModalError" data-connect-el="parentPositionModalVal" class="d-none under-field-error label-sm mt-2"></ul>

                        <input id="primaryEmployeeId" hidden="hidden" />
                        <div class="mt-4">
                            <div id="primaryEmployeeModal" class="select-autocomplete" data-link="@Url.Content($"~/Autocomplite/GetEmployeesByInitials/{Model.Id}/")"
                                 data-autocomplite-type="Employees" data-autocomplite-name="PrimaryEmployeeModal">
                                <input id="primaryEmployeeModalVal" class="form-control" placeholder="@resManager.GetString("PrimaryEmployeeLabel")" />
                            </div>
                        </div>
                        <ul id="primaryEmployeeModalError" data-connect-el="primaryEmployeeModalVal" class="d-none under-field-error label-sm mt-2"></ul>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <input id="createPositionBtn" type="submit" class="btn btn-primary" value="@resManager.GetString("CreateLabel")" />
                <input id="cancelCreationPostitionBtn" type="button" class="btn btn-secondary" data-dismiss="modal" value="@resManager.GetString("CancelLabel")" />
            </div>
        </div>
    </div>
</div>